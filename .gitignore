# Build / install
*.egg-info/
dist/
build/
*.egg

# Python cache
__pycache__/
*.pyc
*.pyo

# Experiments & run outputs
experiments/
jobs/
run*/
misc/

# HuggingFace dataset cache (cache-* shards are transient; data-* shards are real)
datasets/**/cache-*.arrow
datasets/**/*.json.gz
datasets/**/.cache

# Env
.venv/
venv/
env/

# Local machine-specific setup helpers / logs (paths are cluster-specific)
env.sh
install.log

# wandb local run cache (curves live on wandb.ai)
wandb/
